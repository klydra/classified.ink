{
  "name": "api",
  "version": "0.0.0",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch elysia/index.ts",
    "prod": "bun run elysia/index.ts",
    "db": "bun drizzle-kit generate && bun drizzle-kit migrate && bun drizzle-kit push"
  },
  "dependencies": {
    "ulid": "^2.3.0",
    "drizzle-orm": "^0.31.2",
    "@libsql/client": "^0.7.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.22.7"
  },
  "peerDependencies": {
    "typescript": "^5.5.2"
  }
}