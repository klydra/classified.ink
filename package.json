{
  "name": "classified.ink",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=.env --filter '*' dev",
    "build": "bun --env-file=.env --filter 'web' build",
    "prod": "bun --env-file=.env --filter 'api' prod",
    "db": "bun --env-file=.env --filter 'api' db"
  },
  "dependencies": {
    "elysia": "^1.1.3",
    "@elysiajs/eden": "^1.1.1",
    "@elysiajs/cors": "^1.1.0",
    "typescript": "^5.5.4",
    "encryption": "file:./encryption"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^9.7.0"
  },
  "workspaces": [
    "api",
    "web"
  ]
}
