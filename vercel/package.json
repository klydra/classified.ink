{
  "name": "vercel",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../.env --filter 'classified.ink' dev",
    "build": "bun --env-file=../.env --filter 'classified.ink' build"
  },
  "dependencies": {
    "api": "file:../api"
  },
  "workspaces": [
    "..",
    "../api",
    "../web"
  ]
}
