{
  "name": "classified.ink",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=.env --filter '*' dev",
    "build": "bun --env-file=.env --filter '*' build"
  },
  "dependencies": {
    "api": "file:../api"
  },
  "workspaces": [
    "../"
  ]
}
